[pytest]
markers =
    slow: tests that download or run the real reranker model (deselect with -m "not slow")
    integration: tests that need Ollama + the live KG (router-v21 regression; deselect with -m "not integration")
