# Brainstorming visual-companion mockups (local only)
.superpowers/

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg
venv/
.venv/
env/
.env

# Database
*.db
*.db-shm
*.db-wal

# ChromaDB vector store (generated, rebuilt via build_index.py)
chroma_db/

# Logs
*.log
logs/

# Exports
exports/
*.csv

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Pytest
.pytest_cache/
htmlcov/
.coverage

# LaTeX build artifacts
*.aux
*.out
*.toc
*.log
*.fls
*.fdb_latexmk
*.synctex.gz

# Temporary working directories
TEMP CONTENT/

# Node (dashboard loads sql.js/Chart.js from CDN; any local install is cruft)
node_modules/

# DB backups and migration dry-run/dev copies (runtime state, never tracked)
*.backup_*
*.dryrun_*
gsa_gateway_v2dev.db
v2_test.db

# Live tracker state (rewritten every poll)
worldcup_state.json
match_watcher_state.json

# Crawler review output (regenerated each run)
docs/faculty_review.txt

# Brainstorming visual-companion mockups (local only)
.superpowers/

# Reranker model (auto-downloaded once, ~90MB)
models/reranker/

# CPU NLI checker (WS4 bake-off only — downloaded, benchmarked, NOT used in the shipped gate; ~568MB)
models/nli/

# Eval harness run artifacts (regenerated)
eval_results.jsonl
eval_run.log

# eval run outputs (regenerated by scripts/eval.sh)
eval/results.jsonl
eval/results_judged.jsonl

# NJIT crawl build artifacts (regenerated each fetch)
bot/data/sources/njit/**/_people.json
docs/review/*-high-stakes.md

# WorldCup live runtime state (per-deployment, not source)
v2/data/match_watcher_*_state.json

# gate shadow measurement output (regenerable)
eval/gate_shadow.jsonl
eval/gate_shadow_smoke.jsonl
eval/heldout_results.jsonl

# Kavosh auto-eval harness runtime artifacts (source lives in autoeval/*.py + tests)
autoeval/snapshots/
autoeval/autoeval.db
autoeval/status.json
autoeval/report_run_*.md
autoeval/codex_raw/
logs/autoeval.out
logs/autoeval.pid

# FacultyFolio generated output — pages + downloaded headshots belong in the
# separate public Pages repo, NEVER in this code repo. (Self-hosted fonts +
# style.css under facultyfolio/assets/ ARE committed; photos are not.)
/Faculty-Folio/
facultyfolio/assets/photos/
