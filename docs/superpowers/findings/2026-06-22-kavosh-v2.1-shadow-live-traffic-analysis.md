# Kavosh v2.1 — Phase-1b Shadow Run: Live-Traffic Analysis (2026-06-22)

**Status:** Shadow run PARKED (ROUTER_V21=0) for a server shutdown + worldcup→main merge.
Input for the flip-gate review. NOT a flip decision — that's still gated.

## Setup
- Deployed `feat/kavosh-v2.1-phase0-router-bakeoff` to the repo root, `ROUTER_V21=1 ROUTER_V21_SHADOW=1`,
  restarted (2026-06-21 ~20:58). Shadow logs new-vs-current routing only; answers came from the legacy path.
- Real traffic: Mohammad + testers. **192 shadow rows** logged to `logs/router_v21_shadow.jsonl`.
- Mohammad also exported the full chat transcript (question + RENDERED ANSWER) to
  `logs/shadow_qa_2026-06-22.txt` — **169 Q/A pairs parsed** (parser → `/tmp/qa_pairs.json`).
  This is the key extra signal: the shadow log has routing, the transcript has the actual answers.

## Routing agreement (first 172 rows)
- **151 agree / 21 disagree / 0 no-current.**
- Disagreements split into clear router WINS vs. an OPEN QUESTION (below).

## Answer-quality tally (169 rendered answers)
- **131 OK · 35 deflect · 3 error.**

## THE headline: current RAG FABRICATES on structured (KG) questions
Several "top/rank/most/head-of" questions are answered TODAY (via RAG) with confidently WRONG facts —
RAG grabs one arbitrary person from an unrelated (often workshop) doc. The new router sends these to a
KG skill instead. Evidence (question → what current RAG actually answered):
- `top graph people at njit` → "Mohammad Dindoost is a Ph.D. Candidate…" (from a workshop doc) ❌  [new: KG/people_in_org]
- `top njit professor` → "Teik C. Lim is a professor…" — he is the University PRESIDENT ❌
- `most famous ai prof njit` → "Prof. Vincent Oria … multimedia" (cherry-picked) ❌
- `rank database people by papers` → "Shantanu Sharma is an assistant professor…" (not a ranking) ❌
- `head of computer science research ai` → "Baruch Schieber is a professor…" (not the head) ❌
- `most published faculty in ywcc` / `Rank ywcc by faculty count` → "I don't have information" (should aggregate)

These are wrong answers stated as fact, only visible because we have the rendered answers. This is the
single strongest argument for the flip. NOTE for review: confirm the KG skill actually PRODUCES a good
answer for each (routing-right is necessary but not sufficient — some metrics/areas may be thin).

## Two real bugs found (PRE-EXISTING on main — NOT shadow-caused)
1. **Live-fallback HTML-parse crash** — `web_crawler.py:171 clean_text` does `BeautifulSoup(html,"html.parser")`
   with no error handling; a malformed page throws `ParserRejectedMarkup` → user sees "I encountered an error".
   3 hits tonight: `Is pizza event today?` (x2), `professor not reply email what do`. Stack:
   message_handler.py:632 → live_fallback.py:47 → web_crawler.py:173. lxml + html5lib NOT installed.
   FIX (dep-free): try/except around the parse → return "" so it degrades to the normal deflection.
2. **Live-search gate is inconsistent** — current-info questions deflect instead of going live:
   `gym hours today`, `library close what time today`, `Dinning hall is open now`, `free snacks event today`
   said "couldn't find", yet `free pizza` and `campus closed rn?` DID go live. Likely a relevance-threshold
   tuning issue in the live-fallback trigger.
   **CORRECTED DIAGNOSIS (2026-06-22, measured): the threshold hypothesis is WRONG — RECLASSIFIED as a
   CONTENT/coverage item, NOT a gate bug.** Measured top CE rerank scores are ALL far below the 0.15
   threshold (gym 0.003, snacks 0.014, pizza 0.000, campus-closed 0.022), so a threshold change fixes
   nothing. The real divergence is TWO different code paths conflated: (a) `free pizza`/`free snacks` are
   **FOOD intent** → the food-events path (`message_handler.py:451`), which NEVER reaches the live trigger
   (so "pizza went live" was a misread); (b) `gym`/`library`/`dining`/`campus-closed` are non-food → RAG →
   live IS already attempted (CE ≪ 0.15), and a deflection there means the live njit.edu EXTRACTION found no
   groundable page (njit.edu has no cleanly-extractable "gym hours" page) — a content-coverage limit. Fix =
   Bucket-1 content ingestion (authoritative hours/registrar/IT pages) and/or a smarter office-link
   deflection — NOT a one-line gate change. Moved to the content backlog.

## Cosmetic (known)
- Every answer footer says `Kavosh v2.0`; `/start` help still lists retired commands `/events /contact
  /resources`. Stale copy in `bot/connectors/telegram_connector.py` (+ message_handler.py, main.py, gsa_faq.md).
  **RESOLVED (2026-06-22, Mohammad's calls):** (a) `Kavosh v2.0` is NOT stale — it's what's actually live;
  **decision = leave v2.0, bump to v2.1 AS PART OF the router flip** (so the version reflects what's live).
  ~7 code spots to bump AT flip-time: `answer_render.py:13` BRAND, `telegram_connector.py:526` /start,
  `message_handler.py` 41/175/225/245, `ollama_client.py:27`, `feedback.py:227`, + the `test_message_handler.py:210`
  `"v2.0"` assertion. (b) The "help lists retired commands" symptom is **NOT reproducible** in current code or
  the v2 KB — the hardcoded help/identity text lists CAPABILITIES not commands; the v2 `knowledge_items` have
  no `/contact`-`/events`-`/resources` command refs (the strings survive only in v1-legacy `gsa_faq.md`, not in
  the live path). Marked already-addressed/transient. **No build needed for Bug #3 now.**

## The OPEN QUESTION for the flip review (the `OTHER` routes)
Real student questions route to `OTHER`/`COMMAND` under the new router while current sends them to RAG:
`deadline for add drop this semester`, `forgot ucid password`, `how remove financial hold`, `lost id card`,
`what is hold on my account mean`, `last day withdraw class`. **Must confirm `OTHER` does NOT make these
WORSE than today's RAG** (today RAG at least points to the Registrar/office, imperfectly). This is the main
thing to settle before flipping.

## Content vs Routing — the two-bucket framing (Mohammad's insight, confirmed)
The deflect/error list has TWO failure types needing OPPOSITE fixes:
- **Bucket 1 — content gaps (~20):** router was RIGHT (RAG/LIVE), we just lack data. FIXED BY data ingestion +
  better live-search gating. e.g. add/drop deadline, ucid password, gym/library/dining hours, housing, clubs,
  prayer room. (matches [[project_day_to_day_intents]].)
- **Bucket 2 — routing errors (~9):** router was WRONG (sent KG question to RAG → fabrication). FIXED ONLY BY
  routing (v2.1). MORE DATA CAN MAKE THESE WORSE. e.g. the ranking/count/head-of questions above.
- **Hard limit:** semantic search — however good — structurally CANNOT rank/count/aggregate; those ALWAYS
  need a structured skill. So "better search" has a ceiling, and Bucket 2 lives above it.
- Conclusion: the router flip and the content/live work are COMPLEMENTARY, not substitutes — need both.

## RESOLUTION (2026-06-22) — formal metric + flip-gate review + fixes committed
**Clause-2 formal agreement = 0.87** (192/192 comparable, 25 disagreements: 3 RAG→KG wins,
16 RAG→OTHER + 4 RAG→COMMAND + 1 RAG→CLARIFY, 1 KG→RAG).

**The `OTHER`-routes open question is RESOLVED by the code (two expert reviews + direct trace):**
`family=OTHER` (and classifier `COMMAND` with no concrete intent) terminate in the SAME
`_rag_pipeline` call the legacy path uses — so the 16 OTHER + 4 COMMAND disagreements are
label-only and do NOT make student questions worse. Flipping is anti-fab-safe and a modest net
improvement, NOT the "+3 clean wins / 0 regressions" first stated.

**CORRECTED WIN FRAMING (the headline §21 overstated it):** of the 3 RAG→KG wins, only ONE is a
clean structured win — `what is Koutis i10 index → metric_of_person`. `top graph people at njit →
people_in_org(root)` returned a THIN/MISLEADING "1 person: the President" (a near-fabrication), and
`whos the chair for CS → people_by_role` returned EMPTY → degraded to RAG (no-op; CS has no chair
node). One genuine regression found: `roboticswho work on robotics` (KG→RAG) — but ONLY the garbled
token regresses; all clean robotics phrasings route KG.

**FIXES BUILT + REVIEWED + COMMITTED** (branch `feat/kavosh-v2.1-phase0-router-bakeoff`, commit
`a5efa9d`, behind ROUTER_V21 — flip still gated):
1. CLARIFY → RAG in `UnifiedRouter.decide()` (the 1 CLARIFY divergence; abstention OFF in Phase 1b).
2. `people_in_org` root guard in `router.py` (`_is_university_root`) — kills the thin "President"
   answer; sub-orgs still resolve. Shared-router fix → improves legacy path too.
3. robotics regression ACCEPTED-as-known — the proposed fix is Task C3's deliberately-deferred
   net-negative inverse-FN guard; not reopened for one garbled input.
TDD: 5 new tests. Two re-reviews (senior-eng + RAG/anti-fab) = APPROVE.

## Next (when work resumes — all under the expert-review HARD GATE)
1. ✅ DONE — formal agreement metric (0.87) + flip-gate review + the 2 fixes committed.
2. ✅ DONE — `OTHER`-routes open question settled (OTHER===RAG at the handler; flip is safe).
3. **FLIP decision** — remaining gate step: Mohammad's sign-off → redeploy branch + set
   `ROUTER_V21_SHADOW=0` + restart. (The fixes above are the pre-flip conditions; now met.)
4. Batch the two PRE-EXISTING bug fixes + cosmetic (parse crash, live-search gate, footer/help text)
   into ONE reviewed restart — separate from the flip.
5. Bucket-1 content ingestion plan (registrar/IT/bursar/housing/clubs).
