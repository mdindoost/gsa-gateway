discord.py>=2.3.2
python-dotenv>=1.0.0
PyYAML>=6.0.1
rapidfuzz>=3.6.1
aiohttp>=3.9.3
pytest>=7.4.4
pytest-asyncio>=0.23.5
chromadb>=0.4.0
tiktoken>=0.7.0
qrcode[pil]>=7.4
rank-bm25>=0.2.2
python-telegram-bot>=20.0
beautifulsoup4>=4.12.0

# Optional — only for JS-rendered faculty lists (e.g. Data Science headless
# discovery). Not required for CS or the rest of the system. To enable DS:
#   pip install playwright && playwright install chromium
# playwright>=1.40
